%% Test configuration
[
    {rebar3_lfe, [
        {test_mode, true},
        {verbose, true}
    ]},

    {kernel, [
        {logger_level, debug},
        {logger, [
            {handler, default, logger_std_h,
                #{level => debug}}
        ]}
    ]}
].
